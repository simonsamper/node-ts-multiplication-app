interface RunOptions {
    base: number;
    limit: number;
    showTable: boolean;
    name: string;
    destination: string;
}
export declare class ServerApp {
    static run({ base, limit, showTable, name, destination }: RunOptions): void;
}
export {};
//# sourceMappingURL=server-app.d.ts.map